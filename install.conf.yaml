- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.bashrc: bash/bashrc
    ~/.config/hypr: hypr
    ~/.config/fish: fish
    ~/.config/kitty: kitty

- shell:
    - [git submodule update --init --recursive, Installing submodules]
