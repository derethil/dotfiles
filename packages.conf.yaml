# This file contains the necessary packages to install
# for my dotfiles to work properly.

- yay:
    - arch-update # no
    - bkt # no (ags dependency)
    - brightnessctl # no (replaced with brillo)
    - dart-sass # no (ags dependency)
    - libgtop # no (ags dependency)
    - libva-nvidia-driver # no (os dependency)
    - noto-fonts-emoji # no (os dependency I think?)
    - otf-geist-mono-nerd # no (os dependency I think?)
    - uwsm # no (os dependency I think?)
- yay:
    - aylurs-gtk-shell # TODO: oh god
    - yaycache # no
    - yaycache-hook # no

- asdf:
    - plugin: nodejs
      global: latest
      versions:
        - latest

- shell:
    - command: systemctl --user enable --now arch-update.timer
      description: Enabling arch-update timer
